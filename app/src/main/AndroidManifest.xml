<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.lotterysmartapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.DeleteDrawActivity"></activity>
        <activity android:name=".activity.ModifyDrawResultsActivity" />
        <activity android:name=".activity.SellerReservedNumbersActivity" />
        <activity android:name=".activity.HeaderSellerActivity" />
        <activity android:name=".activity.ModifySellerActivity" />
        <activity android:name=".activity.ProfileSellerActivity" />
        <activity android:name=".activity.ProfileUserActivity" />
        <activity android:name=".activity.BuyerReservedNumberActivity"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activity.SellerAddressActivity"
            android:label="@string/title_activity_seller_address" />
        <activity android:name=".activity.ModifyUserActivity" />
        <activity android:name=".activity.TypeUserActivity" />
        <activity android:name=".activity.RegisterSellerActivity" />
        <activity android:name=".activity.SellerListActivity" />
        <activity android:name=".activity.RegisterUserActivity" />
        <activity
            android:name=".activity.AuthenticationActivity"
            android:label="Lottery Smart"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.ForgetPasswordActivity"
            android:label="@string/title_activity_forget_password"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.RegisterDrawActivity"
            android:label="DrawActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.SellerLotteryActivity"
            android:label="SellerLotteryActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>